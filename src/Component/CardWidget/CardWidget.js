import cart from "./assets/carrito_compras.jpg"

const CardWidget = () => {
    return (
        <div>
            <img src={cart} width={40} height={40} alt="cart-widget"/>
            0
        </div>
    )
}

export default CardWidget 